ci_host:
  image: scoutnet/cihost
  environment:
    TYPO3_PATH_WEB: /opt/typo3/web
    SHELL: /bin/sh
  cached: true
  volumes:
    - .:/opt/sh_scoutnet_kalender

build_host:
  image: scoutnet/buildhost
  environment:
    SHELL: /bin/bash
  encrypted_env_file: deployment.env.encrypted
  cached: true
  volumes:
    - .:/opt/data
