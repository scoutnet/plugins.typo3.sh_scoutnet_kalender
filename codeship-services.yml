typo3_test:
  build:
    image: scoutnetdev
    dockerfile_path: Build/Dockerfile.ci
  environment:
    TYPO3_PATH_WEB: /opt/typo3/web
  cached: true
  volumes:
    - .:/opt/typo3/web/typo3conf/ext/sh_scoutnet_kalender

build_host:
  build:
    image: scoutnetbuild
    dockerfile_path: Build/Dockerfile.build
  cached: true
  volumes:
    - .:/opt/data
