plugin.tx_shscoutnetkalender {
	view {
		templateRootPath = {$plugin.tx_shscoutnetkalender.view.templateRootPath}
		partialRootPath = {$plugin.tx_shscoutnetkalender.view.partialRootPath}
		layoutRootPath = {$plugin.tx_shscoutnetkalender.view.layoutRootPath}
	}
	persistence {
		storagePid = {$plugin.tx_shscoutnetkalender.persistence.storagePid}
		recursive = 1
	}
	features {
		# uncomment the following line to enable the new Property Mapper.
		# rewrittenPropertyMapper = 1 
	}

	settings {
		templateFile = {$plugin.tx_shscoutnetkalender.settings.templateFile}
		cssFile = {$plugin.tx_shscoutnetkalender.settings.cssFile}
		limit = {$plugin.tx_shscoutnetkalender.settings.limit}
	}
}
